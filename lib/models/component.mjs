
export class Component {
    
    /**
     * Default-Constructor
     * represents a component (web, angular, ...)
     * @param {*} tag 
     * @param {*} className 
     */
    constructor(tag, className){
        this.tag = tag;
        this.name = className;
    }
}